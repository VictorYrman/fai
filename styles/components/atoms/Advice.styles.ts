// External Dependencies
import { StyleSheet } from "react-native";

// Constants
import { Colors } from "@/constants/theme";

export const AdviceStyles = StyleSheet.create({
    warning: {
        color: Colors.warning
    },
    recommendation: {
        color: Colors.success
    }
});